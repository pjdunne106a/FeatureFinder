[{"Examples":[{"Example1":"<token=word/function>"},
              {"Example2":"<lemma=word/function>"},
              {"Example3":"<postag=word/function>"},
              {"Example4":"<phrase=word/function>"}]},
 {"functions":[{"active":"used to find an active phrase in a piece of text e.g. <phrase=active()>"},
               {"commonword":"used to check whether a word exists within the internal dictionary of words e.g. <token=commonword()>"},
               {"passive":"used to find a passive phrase in a piece of text e.g. <phrase=passive()>"},
               {"auxiliary":"used to find an auxiliary token in a piece of text e.g. <token=auxiliary()>"},
               {"anyword":"used to find any token in a piece of text e.g. <token=anyword()>"},
               {"startswith":"used to find a token that starts with a string e.g. <token=startswith('t')>"},
               {"endswith":"used to find a token that ends with a string e.g. <token=endswith('s')> "},
               {"previous":"used to check whether a string is immediately before a token e.g. <token=previous('the')> "},
               {"next":"used to check whether a string is immediately next to a token e.g. <token=next('like')> "},
               {"notnext":"used to check whether is string is not after a token e.g. <token=notnext('said')> "},
               {"notprevious":"used to check whether is string is not before a token e.g. <token=notprevious('went')> "}]}
               {"validwithoutprefix":"used to check whether a word is valid after removing letters from the start of the word e.g. <token=validwithoutprefix('p')> "}]}
]